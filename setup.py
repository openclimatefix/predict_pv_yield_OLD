from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description="""Use machine learning to map from satellite imagery of clouds 
                to solar PV yield""",
    author='Open Climate Fix',
)
